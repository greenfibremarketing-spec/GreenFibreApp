// src/theme/index.js

// Import all theme parts
import { colors } from './colors';
import { typography, fontFamilies } from './typography';
import { spacing, shadows } from './spacing';

// Export individually
export { colors, typography, fontFamilies, spacing, shadows };

// Export as default object
export default {
  colors,
  typography,
  fontFamilies,
  spacing,
  shadows,
};
